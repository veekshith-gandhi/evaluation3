import { Box } from "@mui/material";
import { useEffect } from "react";

const HomeView = () => {
  useEffect(() => {}, []);
  return <Box sx={{ flexDirection: "row", flexWrap: "wrap" }}></Box>;
};

export default HomeView;
